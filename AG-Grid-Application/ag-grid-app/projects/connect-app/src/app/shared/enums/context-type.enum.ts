export enum ContextType {
  SYNCHRONIZATION = 'Synchronization',
  DATA_SOURCE = 'DataSource',
  TYPE_MAP = 'TypeMap',
  PROJECT_MAP = 'ProjectMap',
  VALUE_MAP = 'ValueMap',
  FILTER = 'Filter',
  SYNC_RULE = 'SyncRule'
}
